const pageTitles = { dashboard: ['Dashboard', 'Saturday, September 5, 2026'], agents: ['Agents', 'Manage your AI publishing agents'], schedule: ['Schedule', 'Manage your publishing calendar'], queue: ['Content queue', '7 items · 3 need review'], youtube: ['YouTube', 'YouTube publishing'], facebook: ['Facebook', 'Facebook publishing'], tiktok: ['TikTok', 'TikTok publishing'], connections: ['Connections', 'Manage connected social accounts'], settings: ['Settings', 'Configure your AgentHub workspace'], account: ['Account', 'Manage your profile and password'] };
export function showPage(page, persist = true) { const info = pageTitles[page]; if (!info) return; document.getElementById('page-title').textContent = info[0]; document.getElementById('page-sub').textContent = info[1]; document.querySelectorAll('.page-section').forEach(section => section.classList.toggle('active', section.id === `page-${page}`)); document.querySelectorAll('[data-page]').forEach(item => item.classList.toggle('active', item.dataset.page === page && item.classList.contains('nav-item'))); document.querySelector('.content').scrollTop = 0; if (persist && window.location.hash !== `#${page}`) window.history.replaceState(null, '', `#${page}`); }
export function initNavigation() { document.querySelectorAll('[data-page]').forEach(item => item.addEventListener('click', () => showPage(item.dataset.page))); const initialPage = window.location.hash.slice(1); showPage(pageTitles[initialPage] ? initialPage : 'dashboard', false); const menu = document.getElementById('user-menu'); const pill = menu?.querySelector('.user-pill'); pill?.addEventListener('click', event => { event.stopPropagation(); menu.classList.toggle('open') }); document.addEventListener('click', event => { if (menu && !menu.contains(event.target)) menu.classList.remove('open') }); }

export function initInteractions() { document.querySelectorAll('[data-action="toggle"]').forEach(toggle => toggle.addEventListener('click', () => toggle.classList.toggle('on'))); document.querySelectorAll('.plat-check').forEach(item => item.addEventListener('click', () => item.classList.toggle('selected'))); document.querySelectorAll('.btn-sm').forEach(button => { if (button.textContent.includes('Disconnect')) button.addEventListener('click', () => { if (confirm('Disconnect this account? Agents will no longer be able to publish to it.')) button.closest('.conn-card')?.remove() }); }); }
